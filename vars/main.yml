ash_ca_key_passphrase: "{{ ca_key_passphrase | default('mypassphrase', true) }}"
ash_ca_filename: "{{ ca_filename | default('ca_cert', true) }}"
ash_ca_destination_path: "{{ ca_destination_path | default('~/generated-certs', true) }}"
ash_ca_cn: "{{ ca_cn | default('Self Signed CA', true) }}"
ash_ca_c: "{{ ca_C | default('US', true) }}"
ash_ca_l: "{{ ca_L | default('Galaxy', true) }}"
ash_ca_o: "{{ ca_O | default('My Company', true) }}"
ash_ca_ou: "{{ ca_OU | default('My Lab', true) }}"
ash_create_archive: "{{ archive_ca | default(false, true) }}"
